{
  "name": "fhe-x402-subgraph",
  "version": "0.1.0",
  "private": true,
  "license": "BUSL-1.1",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph codegen && graph build",
    "deploy": "graph deploy fhe-x402-pool --node https://api.studio.thegraph.com/deploy/",
    "create-local": "graph create fhe-x402-pool --node http://localhost:8020/",
    "deploy-local": "graph deploy fhe-x402-pool --node http://localhost:8020/ --ipfs http://localhost:5001/"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.93.0",
    "@graphprotocol/graph-ts": "^0.35.0"
  }
}
